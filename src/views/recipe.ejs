<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Bake Abroad | <%=recipe.title%></title>
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css"
      rel="stylesheet"
      integrity="sha384-T3c6CoIi6uLrA9TneNEoa7RxnatzjcDSCmG1MXxSR1GAsXEV/Dwwykc2MPK8M2HN"
      crossorigin="anonymous" />
    <!-- <link rel="stylesheet" href="css/custom.css" /> -->
    <link rel="stylesheet" type="text/css" href="/css/custom.css">


  </head>

  <body>
    <header class="navbar navbar-expand-lg navbar">
      <div class="container-fluid">
        <a class="navbar-brand" href="/">
          <h2 class="fs-1 fw-bold" ><%=title%></h2>
        </a>
        <button
          class="navbar-toggler"
          type="button"
          data-bs-toggle="collapse"
          data-bs-target="#navbarNav"
          aria-controls="navbarNav"
          aria-expanded="false"
          aria-label="Toggle navigation">
          <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarNav">
          <ul class="navbar-nav ms-auto">
            <li class="nav-item">
              <a class="nav-link fs-4 active" aria-current="page" href="/">Home</a>
            </li>
            <li class="nav-item">
              <a class="nav-link fs-4 " href="/recipes"> Recipes </a>
            </li>
          </ul>
        </div>
      </div>
    </header>
    <main role="main">
      <!-- <div class="container pt-5"> -->
      <div class="container-fluid pt-3 mt-3">
        <div class="row">
          <div class="col-md-12 col-lg-12 col-sm-12 mt-3">
        <div class="card">
          <div class="card-header">
            <h1><%= recipe.title %></h1>
          </div>
          <ul class="list-group list-group-flush">
            <li class="list-group-item">
              <p><%= recipe.content %></p>
            </li>
            <li class="list-group-item ">
              <ul>
                <% recipe.ingredients.forEach(section => { %>
                  <% if (section.section === 'SPONGE') { %>
                    <h3>Sponge</h3>
                    <h4>Ingredients:</h4>
                    
                      <% section.items.forEach(item => { %>
                        <li><%= item.quantity %> of <%= item.name %></li>
                      <% }); %>
                      <!-- Move the instructions block inside the sponge section -->
                      <h4 class="pt-2">Instructions:</h4>
                      <% recipe.steps.forEach(step => { %>
                        <% if (step.section === 'SPONGE') { %>
                          <% step.steps.forEach(s => { %>
                            <li class="list-group-flush" ><%= s %></li>
                            <% }); %>
                            <% } %>
                            <% }); %>
                  <% } else { %>
                    <!-- do not print the next ul that says instructions -->
                  <% } %>
                <% }); %>
              </ul>
              <li class="list-group-item ">
                <ul>
                  <% recipe.ingredients.forEach(section => { %> <% if
                  (section.section === 'DOUGH') { %> 
                    <h3>Dough</h3>
                    <h4>Ingredients:</h4>
                    <%section.items.forEach(item => { %>
                  <li><%= item.quantity %> of <%= item.name %></li>
                  <% }); %> <% } %> <% }); %>
                </ul>
                <!-- Output steps for FINAL_DOUGH section -->
            <ul>
              <h4 class="pt-2">Instructions</h4>
              <% recipe.steps.forEach(step => { %>
                <% if (step.section === 'FINAL_DOUGH') { %>
                  <% step.steps.forEach(s => { %>
                    <li><%= s %></li>
                    <% }); %>
                    <% } %>
                    <% }); %>
                  </ul>
              </ul>
            </li>
              </ul>
            </li>
            </li>
          </ul>
        </div>
      </div>
      <footer class="container-fluid p-5 text-center">
        <p>
          &copy; This site is created for demonstrative purposes only and does
          not offer any real products or services.
        </p>
        All images and recipes for this project were retrieved from
        <a href="https://amopaocaseiro.com.br/" class="link-info link-offset-2 link-underline-opacity-25 link-underline-opacity-100-hover">
           https://amopaocaseiro.com.br</a>
        </p>
      </footer>

      <!---------- End of Wrapper ----------->

      <script
        src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"
        integrity="sha384-C6RzsynM9kWDrMNeT87bh95OGNyZPhcTNXj1NW7RuBCsyN/o0jlpcV8Qyq46cDfL"
        crossorigin="anonymous"></script>
    </main>
  </body>
</html>
